# Driver-Monitoring-System-
Camera calibration toolkit built with OpenCV (Zhang's method) — estimates intrinsic parameters and lens distortion, validates accuracy via reprojection error, and includes automated tests + CI. Adapted from the calibration pipeline I built for a EuroNCAP-aligned driver monitoring system project.
